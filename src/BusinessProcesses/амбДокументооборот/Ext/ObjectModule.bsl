
Процедура УсловиеПечатиПроверкаУсловия(ТочкаМаршрутаБизнесПроцесса, Результат)
	// Вставить содержимое обработчика.
КонецПроцедуры

Процедура СогласованиеМенеджеромПриСозданииЗадач(ТочкаМаршрутаБизнесПроцесса, ФормируемыеЗадачи, Отказ)
	Для каждого ТекЗадача из ФормируемыеЗадачи Цикл
		ТекЗадача.Исполнитель = Счет.Ответственный;
		ТекЗадача.Счет   =Счет;
		ТекЗадача.Реализация = Реализация;
		ТекЗадача.ВидЗадачи = Перечисления.амбВидыЗадач.Согласование;
		ТекЗадача.КоличествоКопийПечати = КоличествоКопийПечати;
		ТекЗадача.Расписка = Расписка; 
	КонецЦикла;                                 
КонецПроцедуры

Процедура СогласованиеМенеджеромПередСозданиемЗадач(ТочкаМаршрутаБизнесПроцесса, ФормируемыеЗадачи, СтандартнаяОбработка)
	// Вставить содержимое обработчика.
КонецПроцедуры

Процедура Действие1ПередСозданиемЗадач(ТочкаМаршрутаБизнесПроцесса, ФормируемыеЗадачи, СтандартнаяОбработка)
 
КонецПроцедуры

Процедура Действие1ПриСозданииЗадач(ТочкаМаршрутаБизнесПроцесса, ФормируемыеЗадачи, Отказ)
	
	Для каждого ТекЗадача из ФормируемыеЗадачи Цикл
		
		ТекЗадача.Исполнитель = Константы.амбБухгалтерСклада.Получить();
		ТекЗадача.Счет   =Счет;
		ТекЗадача.Реализация = Реализация;
		ТекЗадача.ВидЗадачи = Перечисления.амбВидыЗадач.Распечатка;
		ТекЗадача.КоличествоКопийПечати = КоличествоКопийПечати;
		ТекЗадача.Расписка = Расписка;

	КонецЦикла;
КонецПроцедуры


Процедура СогласованиеМенеджеромПриВыполнении(ТочкаМаршрутаБизнесПроцесса, Задача, Отказ)
	Сообщить("Задача отправлена на склад!!!");
	КоличествоКопийПечати = Задача.КоличествоКопийПечати;
	Расписка = Задача.Расписка;
КонецПроцедуры

Процедура Действие2ПриСозданииЗадач(ТочкаМаршрутаБизнесПроцесса, ФормируемыеЗадачи, Отказ)
	    Администратор = Константы.амбАдминистратор.Получить();
		Для каждого ТекЗадача из ФормируемыеЗадачи Цикл
		ТекЗадача.Исполнитель = Администратор;
		ТекЗадача.Счет   =Счет;
		ТекЗадача.Реализация = Реализация;
		ТекЗадача.ВидЗадачи = Перечисления.амбВидыЗадач.Согласование;
		ТекЗадача.КоличествоКопийПечати = 1;
		ТекЗадача.Расписка = Расписка;
        
		Если не ТекЗадача.Реализация.Контрагент = Константы.НовыйРубеж.Получить()  тогда					
			
			ТекЗадача.ВыполнитьЗадачу();
			
		КонецЕсли;

	КонецЦикла;
КонецПроцедуры


	

